# Example Environment Variables for Joinery Server
# Copy this file to .env and update with your actual values
# NEVER commit .env files with real credentials to version control!

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Use these for production deployments with managed secrets stores

# Application Environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

# Server Configuration
ASPNETCORE_URLS=http://+:5000;https://+:5001
ASPNETCORE_HTTPS_PORT=5001
ALLOWED_HOSTS=your-production-domain.com

# GitHub OAuth Configuration (Production)
Authentication__GitHub__ClientId=your-production-github-client-id
Authentication__GitHub__ClientSecret=your-production-github-client-secret

# Microsoft Entra ID Configuration (Production)
Authentication__Microsoft__TenantId=your-production-tenant-id
Authentication__Microsoft__ClientId=your-production-microsoft-client-id
Authentication__Microsoft__ClientSecret=your-production-microsoft-client-secret

# JWT Configuration (Production)
JWT__SecretKey=your-production-secret-key-for-jwt-tokens-must-be-at-least-256-bits-long
JWT__Issuer=JoineryServer
JWT__Audience=JoineryClients
JWT__ExpirationHours=24

# Database Configuration (Production - if using external database)
ConnectionStrings__DefaultConnection=your-production-database-connection-string

# Logging Configuration (Production)
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft__AspNetCore=Warning
Logging__LogLevel__JoineryServer=Information

# =============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES (for reference)
# =============================================================================
# Use these for local development

# GitHub OAuth Configuration (Development)
# Authentication__GitHub__ClientId=your-dev-github-client-id
# Authentication__GitHub__ClientSecret=your-dev-github-client-secret

# Microsoft Entra ID Configuration (Development)
# Authentication__Microsoft__TenantId=your-dev-tenant-id
# Authentication__Microsoft__ClientId=your-dev-microsoft-client-id
# Authentication__Microsoft__ClientSecret=your-dev-microsoft-client-secret

# JWT Configuration (Development - shorter expiration)
# JWT__SecretKey=development-secret-key-for-jwt-tokens-must-be-at-least-256-bits-long
# JWT__Issuer=JoineryServer-Dev
# JWT__Audience=JoineryClients-Dev
# JWT__ExpirationHours=1